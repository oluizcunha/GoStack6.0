{% extends "_layouts/default.njk" %}

{% block body %}
  <div class= "content">
    <strong> Eai, {{user.name}}</strong>
    <p>Inicie um agendamento escolhendo:</p>

    <ul class="providers">
      {% for provider in providers %}
        <li class="provider">
          <div>
            <img src=""/>
            <strong>{{ provider.name }}</strong>
          </div>
          <a href="">+</a>
        </li>
      {% endfor %}
    </ul>

    <a href="/app/logout" class="logout">sair</a>
  </div>
{% endblock %}